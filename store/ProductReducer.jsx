export const initialeducervalue ={
    name:"",
    age:"",
    accept:false
}

export const FormReducer=(state,action)=>{
    console.log(action.payload.current.id,action.payload.current.value)
    switch(action.type){
        
    }
}